#pragma once

/// <summary>
/// Modified: Kaylon Riordan D00255039
/// Added a layer for walls, also renamed air layers to tanks and particles to better reflect game
/// </summary>

enum class SceneLayers
{
	kBackground,
	kWalls,
	kParticles,
	kTanks,
	kLayerCount
};